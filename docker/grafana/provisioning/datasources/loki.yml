# Grafana datasource provisioning.
# This file is automatically loaded on first boot — no manual UI setup needed.

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    version: 1
    editable: true
    jsonData:
      maxLines: 1000
